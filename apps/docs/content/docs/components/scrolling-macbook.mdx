---
title: "Scrolling Macbook"
description: Scrolling Macbook with hover effect.
---

# Expandable Card

<ComponentPreview component={<ScrollingMacbookDemo /> } className="p-0 mb-20" fileName="scrolling-macbook" />

### Installation

<Tabs defaultValue="manual">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>

  <TabsContent value="cli">
    <Steps>
      <Step>Run the following command</Step>
      ```bash
      npx shadcn@latest add https://ui.eunary.com/registry/scrolling-macbook.json
      ```
    </Steps>
  </TabsContent>

  <TabsContent value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      ```bash
      npm install motion clsx tailwind-merge
      ```
      <Step>Add util file</Step>
      <Callout>lib/utils.ts</Callout>

      ```tsx
      import { ClassValue, clsx } from 'clsx';
      import { twMerge } from 'tailwind-merge';

      export function cn(...inputs: ClassValue[]) {
          return twMerge(clsx(inputs));
      }
      ```

      <Step>Copy the source code</Step>
      <Callout>components/ui/scrolling-macbook.tsx</Callout>
     
      <ComponentSource className="" collapsible={true}/>
    </Steps>
  </TabsContent>
</Tabs>



### Props

Use the following props to customize the tooltip.

| Prop                | Type                     | Description                                        |
| :------------------ | :----------------------- | :------------------------------------------------- |
| <Callout>children</Callout>          | <Callout>React.ReactNode</Callout>          | The child elements to be rendered on the macbook display.|
| <Callout>className</Callout>         | <Callout>string</Callout>                   | The CSS class to be applied to the card.           |


<ExploreComponents currentComponent="scrolling-macbook" />