'use client';

import ThemeToggler from '@/components/ui/theme-toggler';

const button = () => {
    return (
        <div>
            <ThemeToggler />
        </div>
    );
};

export default button;
